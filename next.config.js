/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'encrypted-tbn0.gstatic.com',
            'files.edgestore.dev'
        ]
    }
}

module.exports = nextConfig
